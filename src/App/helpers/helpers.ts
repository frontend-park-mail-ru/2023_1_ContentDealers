// Handlebars.registerHelper('stringItems', function(items, field) {
//     return items.map((item: any) => {
//         return item[field][0].toUpperCase() + item[field].slice(1);
//     }).join(', ');
// });
